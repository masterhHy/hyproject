<view class='scroll-view-item_H'>
  <i-scroll binddownLoading="downLoading" bindupLoading="upLoading" isAjax="{{isAjax}}" url="{{url}}" method="{{method}}" param="{{param}}" pageSize="{{pageSize}}">
    <slot></slot>
  </i-scroll>
</view>

